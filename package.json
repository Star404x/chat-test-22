{
  "name": "portfolio2",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://<GITHUB_USERNAME>.github.io/<REPO_NAME>",
  "scripts": {
    "start": "live-server src --port=3000 --open=./index.html",
    "deploy:gh": "gh-pages -d src",
    "deploy:netlify": "netlify deploy --dir=src --prod"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "live-server": "^1.2.1",
    "netlify-cli": "^10.0.0"
  }
}
