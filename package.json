{
  "name": "mini-shoes-site",
  "version": "1.0.0",
  "private": true,
  "description": "Mini Shoes Site - demo and small store",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node dist/index.js",
    "build": "esbuild src/index.js --bundle --platform=node --target=node14 --outfile=dist/index.js --minify",
    "optimize": "npm run build",
    "test": "jest --runInBand",
    "lint": "eslint . --ext .js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "eslint": "^8.0.0",
    "nodemon": "^2.0.22"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
