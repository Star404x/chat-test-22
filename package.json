{
  "name": "leather-mini-site",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo \"No build step defined; add bundler if needed\"",
    "lint": "echo \"Add eslint or other linters\"",
    "test": "echo \"No tests configured\"",
    "ci": "npm run lint && npm test && npm run build",
    "deploy": "echo \"Configure deploy script (gh-pages/vercel)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "compression": "^1.7.4",
    "helmet": "^6.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "eslint": "^8.45.0"
  },
  "engines": {
    "node": ">=14"
  }
}
