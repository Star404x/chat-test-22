{
  "name": "mini-photo-site",
  "version": "0.1.0",
  "description": "Small adaptive photo site backend for uploads, storage and simple gallery",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo \"No build step for backend-only project\"",
    "start:dev": "NODE_ENV=development node src/index.js"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "morgan": "^1.10.0"
  }
}
