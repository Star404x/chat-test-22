{
  "name": "exp-api-test",
  "version": "0.1.0",
  "description": "Minimal Express API for testing endpoints",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest --runInBand",
    "ci": "npm test",
    "release": "npm version patch -m \"release %s\" && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "nodemon": "^2.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}
