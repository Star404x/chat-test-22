{
  "name": "leather-site",
  "version": "1.0.0",
  "description": "Мини-сайт Leather — презентация и продажа кожаных изделий",
  "main": "src/index.js",
  "scripts": {
    "build": "node src/index.js build",
    "start": "node src/index.js start",
    "dev": "node src/index.js start",
    "test": "echo \"No unit tests configured\" && exit 0",
    "e2e": "npx playwright test --project=chromium --project=firefox --project=webkit",
    "lint": "eslint . || true"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "playwright": "^1.35.0"
  },
  "engines": {
    "node": ">=16"
  }
}
