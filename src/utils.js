module.exports = {
  // Возвращает строковое представление входного значения
  echo: (value) => String(value),

  // Простая функция суммирования, приводящая аргументы к числам
  sum: (a, b) => Number(a) + Number(b)
};
